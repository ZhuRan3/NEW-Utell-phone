# 手机端 Agent 工作规则

## 任务范围

本仓库只负责 Zhu3xx 手机端：iOS 原生 + SwiftUI，最低 iOS 17（Q-PH-2026-012/013 已确认）。本端范围包括手机安全存储、配对体验、链路状态、文字日志输入、Card Projection 展示与确认/纠正、手机侧诊断和发布门禁。

Relay 服务、电脑 Connector、Pi Agent、权威数据库和电脑端安装运维不在本仓库实现。需要跨端联调时，只消费 `文档/架构与契约/` 和 `harness/契约/` 中的版本化公开契约。

## 必读顺序

```text
AGENTS.md
  -> 项目总体背景愿景目标/PRD：Utell（油条）第一代智能日志 MVP.md
  -> 文档/背景与链路/背景复核方法与工作链路.md
  -> 文档/架构与契约/架构基线.md
  -> 文档/架构与契约/跨端公开契约治理规范.md
  -> 文档/工作进度/开发进度与会话交接.md
  -> 开发规则规范/README.md
  -> 当前任务相关的需求、Q&A、契约和 harness 场景
```

## 权威性

1. 用户明确回答并写入 Q&A 的结论高于工程建议。
2. 当前 PRD 高于未写入本仓库的外部材料。
3. 背景复核文件只说明方法，不代表任何外部结论已被采用。
4. 发现文档、契约和运行时行为不一致时，先登记冲突，不静默修正。

## 硬约束

- 手机不得把 Relay ACK 展示成“电脑已接收”“已提交”或“已投影”。
- 按当前 PRD，手机不持久化业务原文、Outbox、业务卡片缓存和自动重发任务。
- 未进入 READY 时禁用业务输入；未知结果不得自动重发。
- Card Command 必须在线并携带 `event_id`、命令类型和 `expectedVersion`。
- 不引入通用 Shell/HTTP 执行能力，不把 Pi runtime 嵌入手机。
- 机读契约、fixtures 和证据可共享；不共享另一端运行时领域代码。
- 两端 `harness/契约/integration-profile.yaml` 必须逐字节一致；契约变更按跨端公开契约治理规范执行。

## 文档要求

需求变更先写 `文档/需求对齐/需求问答记录.md`，保留用户原文、选项、回答和影响；再同步 PRD、架构说明、测试场景和变更记录。中文说明优先，协议字段、代码标识和命令保留原文。
