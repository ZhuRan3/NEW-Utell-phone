# 需求确认台账

> 建立日期：2026-08-23
> 用途：把所有需求与选型拆成可单独确认的条目，逐项与用户确认。每条确认后按 `开发规则规范/01-需求对齐与Q&A规范.md` 在 `需求问答记录.md` 留下完整 Q&A，再回写本台账状态。
> 状态口径：待确认 / 用户已确认 / 暂定建议 / 已否决 / 已变更

## A. 链路与数据归属（跨端共同，两端台账一致）

| 编号 | 条目 | 对应 PRD | 功能对应 | 状态 |
| --- | --- | --- | --- | --- |
| A1 | 四层链路：手机 ↔ Relay ↔ PC Connector ↔ Utell Pi | 0.1/0.2 | 全链路 | 用户已确认（PRD 依据） |
| A2 | 数据权威在 Connector；手机业务数据仅存内存，禁持久化 | 0.2/2.2/7.3 | 提交、时间线、诊断 | 用户已确认（PRD 依据） |
| A3 | 应用层 E2EE 在端点终止；Relay 只见密文、不持密钥 | 0.2/7.3 | 传输安全 | 用户已确认（PRD 依据） |
| A4 | 一用户配对一台电脑 + 一台手机；不多设备不共享 | 2.3 | 配对、账号 | 用户已确认（PRD 依据） |
| A5 | Relay 无离线队列；未即时投递即丢弃并报错 | 7.2/FR-008 | 提交失败语义 | 用户已确认（PRD 依据） |
| A6 | Relay ACK ≠ 电脑接收/提交/投影 | 2.1/7.4 | 状态展示 | 用户已确认（PRD 依据） |

## B. 手机端需求

| 编号 | 条目 | 对应 PRD | 功能对应 | 状态 |
| --- | --- | --- | --- | --- |
| B1 | 配对流程：扫码、指纹核对、一次性 token（建议 5 分钟） | FR-001 | 配对页 | 用户已确认 |
| B2 | READY 门禁与七态链路状态机；非 READY 禁输入 | 5.2/FR-002/FR-003 | 输入框、状态区 | 用户已确认（PRD 依据） |
| B3 | 提交状态机 DRAFT→SENDING→UNKNOWN_RESULT→RECEIVED；不自动重发 | 5.3/FR-002 | 提交体验 | 用户已确认（PRD 依据） |
| B4 | 时间线与卡片：按日分组、原文追溯、时间待确认分组、只接受更高版本 | FR-004/5.4 | 时间线页 | 用户已确认（PRD 依据） |
| B5 | 确认/纠正：在线、携带 event_id+expectedVersion、VERSION_CONFLICT 处理 | FR-005/5.4 | 卡片详情 | 用户已确认（PRD 依据） |
| B6 | Projection 同步时机与内存游标（不持久化游标） | 5.5 | 时间线数据流 | 用户已确认（PRD 依据） |
| B7 | 设置与诊断：配对信息、PARSE_FAILED 重试入口、脱敏导出、撤销配对 | FR-007 | 设置页 | 用户已确认（PRD 依据） |
| B8 | P0 不做：提醒、语音、多模态、搜索、Chat/RAG、完成/删除/分享/批量 | 2.2 | 范围门禁 | 用户已确认（PRD 依据） |

## C. 云端需求（详见 server 仓库台账，此处仅交叉引用）

Relay 职责面、审计留存期限、错误码、健康检查节奏、单主原子性等条目在 NEW-Utell-server 台账维护；涉及手机可见语义的（错误码、链路状态）与本端 B2/B3 联动确认。

## D. 技术选型

| 编号 | 条目 | 关联决策包 | 状态 |
| --- | --- | --- | --- |
| D1 | 首发平台范围（仅 iOS / iOS+Android 同期） | TS-PH-2026-001 子问题 (a) | 待确认 |
| D2 | 手机 UI 技术栈与最低系统版本 | TS-PH-2026-001 | 待确认 |
| D3 | Relay 语言/边缘代理/元数据存储/云服务商 | TS-SV-2026-001（server 仓库） | 待确认 |
| D4 | E2EE 协议套件与跨端测试向量（需 Painfox 共同接受） | 待建 | 待确认 |
| D5 | 账号认证方式与 Provider | 待建 | 待确认 |

## E. 指标与发布

| 编号 | 条目 | 对应 PRD | 状态 |
| --- | --- | --- | --- |
| E1 | P0 指标阈值（配对 ≥90%、接收 ≥99%、P95 ≤5s/≤15s、解析 ≥95%、原文残留 0） | 1.3 | 待确认 |
| E2 | 发布顺序、灰度与回滚策略 | 9.1 | 待确认 |

## 确认顺序建议

A（链路骨架）→ B（本端需求）→ D（选型）→ E（指标发布）。C 类在 server 仓库同步进行。每确认一条：更新本台账状态 → 写 Q&A 记录 → 回写架构基线/PRD 细分文档。
